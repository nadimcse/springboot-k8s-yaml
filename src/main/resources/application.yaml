spring:
  application:
    name: spring-boot-k8s
  cloud:
    kubernetes:
      client:
        namespace: test

tracing:
  url: http://simple-prod-collector.jaeger.svc.cluster.local:4317

management:
  tracing:
    sampling:
      probability: 1.0
#tracing:
#  url: http://localhost:4317